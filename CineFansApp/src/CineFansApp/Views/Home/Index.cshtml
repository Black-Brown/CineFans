@using CineFansApp.Web.ViewModels
@model HomeViewModel

@{
    ViewData["Title"] = "Inicio";
}

<div class="row">
    <div class="col-md-8">
        @if (User?.Identity?.IsAuthenticated == false)
        {
            <div class="alert alert-info mb-4">
                <h4>¡Bienvenido a CineFans!</h4>
                <p>La red social para amantes del cine. <a asp-controller="Account" asp-action="Register">Regístrate</a> o <a asp-controller="Account" asp-action="Login">inicia sesión</a> para compartir tus opiniones sobre películas.</p>
            </div>
        }

        @* Aquí puedes agregar el código para las publicaciones y demás en el futuro *@
    </div>

    <div class="col-md-4">
        <div class="sidebar">
            @* En este espacio puedes mostrar cosas como películas populares o sugerencias de usuarios en el futuro *@
        </div>
    </div>
</div>
